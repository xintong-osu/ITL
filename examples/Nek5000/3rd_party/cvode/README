# To build CVODE run the following commands:

tar -zxvf cvode-2.5.0.tar.gz
patch -p1 < longlong_int-2.5.0.patch
cd cvode-2.5.0 

# PGI example
./configure --prefix=$HOME/cvode-2.5 CC=pgcc F77=pgf77 \ 
            --with-mpicc=mpicc --with-mpif77=mpif77

make
make install

